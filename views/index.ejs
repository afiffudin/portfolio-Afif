
<!doctype html>
<html lang="id">
  <!-- <%- include('partials/navbar') %> -->
  <head>
    <!-- Font Awesome CDN -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Portfolio — Muhamad Afifudin Yusup</title>

  <!-- Tailwind (CDN untuk testing cepat; jika sudah build, hapus ini) -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Swiper CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />
  <style>
    /* sedikit styling bantu agar slide selalu ter-center */
    .myPortfolio { padding-bottom: 2rem; }
    .swiper-slide { display: flex; justify-content: center; align-items: flex-start; }
  </style>
</head>
<body class="bg-gray-50 text-gray-800">
<!-- Social Media Fixed Top-Left -->
<div class="fixed top-4 left-4 flex flex-col gap-3 z-50">
  <a href="https://www.linkedin.com/in/afif-dev-b8b05a15a" target="_blank" class="flex items-center justify-center w-10 h-10 bg-blue-600 text-white rounded-full hover:bg-blue-800 transition">
    <i class="fab fa-linkedin"></i>
  </a>
  <a href="https://wa.me/6289638243059" target="_blank" class="flex items-center justify-center w-10 h-10 bg-green-500 text-white rounded-full hover:bg-green-700 transition">
    <i class="fab fa-whatsapp"></i>
  </a>
  <a href="https://github.com/afiffudin" target="_blank" class="flex items-center justify-center w-10 h-10 bg-gray-800 text-white rounded-full hover:bg-black transition">
    <i class="fab fa-github"></i>
  </a>
  <a href="https://gitlab.com/afiffudin" target="_blank" class="flex items-center justify-center w-10 h-10 bg-red-600 text-white rounded-full hover:bg-red-800 transition">
    <i class="fab fa-gitlab"></i>
  </a>
</div>

  <!-- Hero Section -->
  <section id="hero" class="py-12">
    <div class="container mx-auto px-4 grid md:grid-cols-2 gap-8 items-center">
      <div>
        <h1 class="text-4xl font-extrabold mb-4">Halo, saya Muhamad Afifudin Yusup</h1>
        <p class="text-lg text-gray-700 mb-6">
          Saya seorang developer dan IT specialist. Berikut contoh proyek dan pengalaman kerja saya.
        </p>
        <a href="M_Afiffudin_Yusup.pdf" class="inline-block px-6 py-3 bg-indigo-600 text-white rounded-lg shadow" id="downloadCV">
  Unduh CV
</a>

<script>
  document.getElementById('downloadCV').addEventListener('click', function(e){
    e.preventDefault();
    const link = document.createElement('a');
    link.href = 'M_Afifudin Yusup_CCNA.pdf'; // path file
    link.download = 'M_Afifudin Yusup_CCNA.pdf';
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
  });
</script>

      </div>
      <!-- Sosial Media dengan Logo -->
      <div class="rounded-lg overflow-hidden shadow-lg">
        <img src="/images/Logo_New_1.png" alt="Hero" class="w-80 h-39 object-cover" />
      </div>
    </div>
  </section>

  <!-- About Section -->
  <section class="px-6 py-12 bg-gray-900 text-white">
  <h2 class="text-3xl font-bold mb-6 text-center text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 via-purple-500 to-pink-500">
    Deskripsi Profesional & Pengalaman
  </h2>

  <!-- Tempat teks animasi -->
  <div id="typing-text" class="text-xl font-mono text-center h-24 text-cyan-400"></div>
</section>

<!-- Tambahkan GSAP + TextPlugin -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/TextPlugin.min.js"></script>
<script>
  gsap.registerPlugin(TextPlugin);

  const texts = [
    "💻 Berpengalaman dalam IT Support, Instalasi CCTV & Networking (Climping RJ45, Splicing Fiber Optic) dan konfigurasi menggunakan Cisco.",
    "📱 Pengembangan Aplikasi Mobile (Flutter).",
    "🐳 Membuat container App, DB, Networking di dalam Docker.",
    "⚙️ Implementasi Sistem & Pengujian Sistem (SIMRS)."
  ];

  let tl = gsap.timeline({ repeat: -1, repeatDelay: 1 });

  texts.forEach((txt) => {
    tl.to("#typing-text", { 
      duration: 3, 
      text: txt, 
      ease: "none",
      color: gsap.utils.random(["#22d3ee","#22c55e","#eab308","#ec4899"]) // random warna tiap teks
    })
    .to("#typing-text", { opacity: 0, duration: 0.8 }, "+=2") // tahan 2 detik, lalu fade out
    .to("#typing-text", { opacity: 1, duration: 0.2 });       // fade in sebelum teks berikutnya
  });
</script>

  <!-- Portfolio Section (Swiper coverflow) -->
  <section id="portfolio" class="py-12">
    <div class="container mx-auto px-4">
      <h2 class="text-2xl font-bold mb-6 text-center">Portfolio</h2>

      <div class="swiper myPortfolio">
        <div class="swiper-wrapper">

          <!-- Slide 1 -->
          <div class="swiper-slide max-w-xs">
            <a href="/portfolio/1" class="block bg-white rounded-xl shadow-lg overflow-hidden border border-gray-200 hover:shadow-2xl transition">
              <img src="/images/Flutter1.png" alt="Project 1" class="w-full h-56 object-cover" />
              <div class="p-4">
                <h3 class="font-semibold text-lg">Programming Mobile</h3>
                <p class="text-sm text-gray-600">Deskripsi singkat Programming — teknologi: Flutter / Laravel / Boostrap.</p>
              </div>
            </a>
          </div>

          <!-- Slide 2 -->
          <div class="swiper-slide max-w-xs">
            <a href="/portfolio/2" class="block bg-white rounded-xl shadow-lg overflow-hidden border border-gray-200 hover:shadow-2xl transition">
              <img src="/images/ITSupport.png" alt="Project 2" class="w-full h-56 object-cover" />
              <div class="p-4">
                <h3 class="font-semibold text-lg">IT Support</h3>
                <p class="text-sm text-gray-600">Deskripsi singkat IT Support  — CCTV, Infra Server & Networking.</p>
              </div>
            </a>
          </div>

          <!-- Slide 3 (opsional) -->
          <div class="swiper-slide max-w-xs">
            <a href="/portfolio/3" class="block bg-white rounded-xl shadow-lg overflow-hidden border border-gray-200 hover:shadow-2xl transition">
              <img src="/images/DevOps.jpg" alt="Project 3" class="w-full h-56 object-cover" />
              <div class="p-4">
                <h3 class="font-semibold text-lg">DevOps </h3>
                <p class="text-sm text-gray-600">Deskripsi singkat proyek 3 — teknologi: Docker Membuat Container APP | Databases</p>
              </div>
            </a>
          </div>
 <!-- Slide 4 (opsional) -->
          <div class="swiper-slide max-w-xs">
            <a href="/portfolio/4" class="block bg-white rounded-xl shadow-lg overflow-hidden border border-gray-200 hover:shadow-2xl transition">
              <img src="/images/Jasa_freelance.jpeg" alt="Project 3" class="w-full h-56 object-cover" />
              <div class="p-4">
                <h3 class="font-semibold text-lg">Jasa Teknis ( Support & Deploy To Host ) </h3>
                <p class="text-sm text-gray-600">Deskripsi singkat Freelance — teknologi: Flask Framework Python | Databases</p>
              </div>
            </a>
          </div>
          <!-- Tambah slide lain sesuai kebutuhan -->
        </div>

        <!-- Pagination -->
        <div class="swiper-pagination mt-4"></div>
      </div>
    </div>
  </section>

  <!-- Contact Section -->
  <section id="contact" class="py-12">
    <div class="container mx-auto px-4">
      <h2 class="text-2xl font-bold mb-4">Form Kontak</h2>
      <form action="/contact" method="post" class="max-w-xl bg-white/80 rounded-lg p-6 shadow-lg">
        <label class="block mb-3">
          <span>Nama</span>
          <input required type="text" name="name" class="w-full mt-1 p-2 border rounded" placeholder="Nama Anda" />
        </label>
        <label class="block mb-3">
          <span>Email</span>
          <input required type="email" name="email" class="w-full mt-1 p-2 border rounded" placeholder="email@domain.com" />
        </label>
        <label class="block mb-3">
          <span>Pesan</span>
          <textarea required name="message" rows="5" class="w-full mt-1 p-2 border rounded" placeholder="Tulis pesan..."></textarea>
        </label>
        <button type="submit" class="px-4 py-2 bg-indigo-600 text-white rounded">Kirim Pesan</button>
      </form>
    </div>
  </section>
<!-- Feedback Display Section -->
  <section id="feedbackDisplay" class="py-12 px-6 bg-gray-50 container mx-auto max-w-3xl">
    <h2 class="text-2xl font-bold mb-6 text-center">Testimoni & Penilaian User</h2>
    <div id="feedbackList" class="space-y-4">
      <!-- feedback akan diisi via JS -->
    </div>
    <div class="text-center mt-6">
      <a href="/feedback" class="inline-block px-6 py-2 bg-indigo-600 text-white rounded-lg shadow hover:bg-indigo-700">
        Beri Feedback
      </a>
    </div>
  </section>
  <script src="/js/feedback-display.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>


  
  <!-- Swiper JS -->
  <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>

  <!-- Inisialisasi Swiper dengan penyesuaian jumlah slide -->
  <script>
    document.addEventListener('DOMContentLoaded', function () {
      const slides = document.querySelectorAll('.myPortfolio .swiper-slide');
      const slidesCount = slides.length;

      // Jika tidak ada slide, hentikan
      if (slidesCount === 0) return;

      // Konfigurasi dinamis
      const isFew = slidesCount < 3; // < 3 => gunakan mode numeric slidesPerView
      const swiperOptions = {
        effect: 'coverflow',
        grabCursor: true,
        centeredSlides: true,
        slidesPerView: isFew ? 1 : 'auto',
        coverflowEffect: {
          rotate: isFew ? 18 : 30,
          stretch: 0,
          depth: 140,
          modifier: 1,
          slideShadows: true,
        },
        loop: slidesCount > 1, // loop hanya jika ada minimal 2 slide
        autoplay: slidesCount > 1 ? {
          delay: 3000,
          disableOnInteraction: false
        } : false,
        pagination: {
          el: '.swiper-pagination',
          clickable: true
        },
        breakpoints: {
          640: { slidesPerView: Math.min(1, slidesCount) },
          768: { slidesPerView: Math.min(2, slidesCount) },
          1024: { slidesPerView: Math.min(3, slidesCount) }
        }
      };

      // Buat swiper
      const mySwiper = new Swiper('.myPortfolio', swiperOptions);

      // Jika cuma 1 slide, sembunyikan pagination (opsional)
      if (slidesCount === 1) {
        const pag = document.querySelector('.swiper-pagination');
        if (pag) pag.style.display = 'none';
      }
    });
  </script>
</body>
</html>
